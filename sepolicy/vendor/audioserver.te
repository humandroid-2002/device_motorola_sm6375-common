# Audio effects (Viper4Android / JamesDSP)

get_prop(audioserver, vendor_audio_prop)

allow audioserver unlabeled:file {
    getattr
    open
    read
    write
};

allow hal_audio_default hal_audio_default:process execmem;
